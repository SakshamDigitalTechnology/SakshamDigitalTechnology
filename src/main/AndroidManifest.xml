<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="technology.digital.saksham">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".fragment.SyllbusFragment"></activity>
        <activity android:name=".fragment.InfoFragment" />
        <activity
            android:name=".fragment.ViewAllFragment"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_view_all_fragment"
            android:theme="@style/FullscreenTheme" />
        <activity android:name=".adapter.CourseFragmentAdapter" />
        <activity android:name=".adapter.MarketingAdapter" />
        <activity android:name=".fragment.MarketingFragment" />
        <activity android:name=".adapter.MobileAppAdapter" />
        <activity android:name=".fragment.MobileAppFragment" />
        <activity android:name=".adapter.WebDevelopmentAdapter" />
        <activity android:name=".fragment.WebDevelopmentFragment" />
        <activity android:name=".adapter.AllTechnologyAdapter" />
        <activity android:name=".fragment.AllTechnologyFragment" />
        <activity android:name=".adapter.HomeFragmentRecyclerAdapter" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.CourseActivity"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".activity.HomeActivity"
            android:theme="@style/AppTheme1" />
        <activity android:name=".hintActivity.SlideActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>